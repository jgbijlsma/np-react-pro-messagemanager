# np-react-pro-messagemanager

Om het project te starten, open een terminal in deze folder en voer `npm install` uit. Daarna kan je met `npm run dev` een development server starten die de app draait.
